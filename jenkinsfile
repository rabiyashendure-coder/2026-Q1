pipeline {
    agent any

    stages {
        stage('Checkout Code') {
            steps {
                git branch: 'master', 
                url: 'https://github.com/rabiyashendure-coder/2026-Q1.git'
            }
        }

        stage('Build master') {
            steps {
                echo "Building master branch"
                sh 'ls -l'
            }
        }

        stage('Archive File') {
            steps {
                archiveArtifacts artifacts: 'index.html', fingerprint: true
            }
        }
    }
}
