pipeline {
    agent any

    stages {
        stage('Checkout Code') {
            steps {
                git branch: 'Q1', 
                url: 'https://github.com/rabiyashendure-coder/2026-Q1.git'
            }
        }

        stage('Build Q1') {
            steps {
                echo "Building Q1 branch"
                sh 'ls -l'
            }
        }

        stage('Archive File') {
            steps {
                archiveArtifacts artifacts: 'index.html', fingerprint: true
            }
        }
    }
}
